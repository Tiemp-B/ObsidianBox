# 로봇 모션제어 인터페이스 프로그램 — 문제은행 발췌

출처: `자료/문제모음.md` (정답 없음, 도메인 지식으로 정답 판단 필요). 이 세세항목으로 분류된 문항 1개.

---

**390.** 로봇 제어 프로그램에서 위치 명령 큐(Queue)를 사용하는 목적으로 가장 적절한 것은?
① 모터 드라이버의 전류 제한을 위해
② 모션 명령의 버퍼링과 연속 실행을 위해
③ 모터의 최대 속도를 증가시키기 위해
④ 위치 피드백 값을 저장하기 위해
