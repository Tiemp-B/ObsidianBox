## 증상
클로드를 실행했는데 클로드 창이 안켜진다
컴퓨터를 재부팅해도, 프로세스 종료 재시작해도 안켜진다

## 해결방법
```powershell
# Claude와 네이티브 호스트 프로세스 종료
Get-Process *claude* -ErrorAction SilentlyContinue | Stop-Process -Force
Get-Process *chrome-native-host* -ErrorAction SilentlyContinue | Stop-Process -Force

# 이걸 스크립트로 실행하는 경우, 핸들이 해제될 때까지 잠시 기다린 후 삭제해
Start-Sleep -Seconds 2

# 마지막으로, 실제 정리 작업
Remove-Item -Recurse -Force "$env:LOCALAPPDATA\AnthropicClaude\app-1.1.4173"
```

## 내용
최신의 문제 있는 버전을 복원하는 과정