# 로봇소프트웨어개발기사 학습 노트 프로젝트 — 핸드오프 문서 (v2)

이 문서는 claude.ai 세션에서 진행한 작업을 **별도 환경의 Claude Code**로 이어가기 위한 인계 자료다.
작업 대상은 Obsidian 볼트이며, **Git 레포지토리로 관리된다.**

- **Git 레포**: https://github.com/Tiemp-B/ObsidianBox.git

이전 세션에서는 Obsidian 볼트에 `obsidian-mcp-tools`(로컬 REST API 기반 MCP 서버)를 통해서만 접근했다. Claude Code에서는 이 레포를 clone한 로컬 경로에 **직접 파일시스템으로 접근**하게 될 것이다.

---

## 1. 프로젝트 개요

**목표**: "로봇소프트웨어개발기사" 필기시험 대비 학습 노트를 Obsidian 볼트에 구축.
두 축의 산출물이 있다.

1. **문제은행 JSON 파일** — 과목별 기출/예상 문제를 분류해 저장 (선택지·해설은 제거된 "지문만" 버전)
2. **코넬노트 스타일 Markdown 학습 페이지** — 세세항목별로 실제 개념 설명 콘텐츠 작성. 문제은행의 문제들을 **소재/용어 힌트로만** 활용하고, 그 자체를 인용하지는 않음(4절 참고)

4개 과목: 1.로봇운영소프트웨어 / 2.로봇소프트웨어구조설계 / 3.모션소프트웨어 / 4.지능소프트웨어

---

## 2. Obsidian 볼트 구조

```
지식창고/
└── 로봇소프트웨어개발기사/
    ├── 로봇소프트웨어개발기사.md                (최상위 인덱스, 거의 비어있음)
    ├── 로봇운영소프트웨어/
    │   ├── 로봇운영소프트웨어.md                 (인덱스, 비어있음)
    │   └── 1. 로봇 운영 소프트웨어 개발/         (=주요항목, 1과목은 주요항목이 1개뿐)
    │       ├── 1. 로봇 운영 소프트웨어 개발.md
    │       ├── 로봇소프트웨어개발기사_필기_1과목_문제은행_1.json   (선택지·해설 제거됨)
    │       ├── 1. 모니터링 툴 개발/              (=세부항목)
    │       │   ├── 1. 모니터링 툴 개발.md
    │       │   ├── 1. 로봇 제어기 특성/          (=세세항목)
    │       │   │   ├── 1. 로봇 제어기 특성.md    (빈 인덱스 노트)
    │       │   │   ├── 1. 로봇제어기.md                              완료 (개요/MOC)
    │       │   │   ├── 2. 실시간 운영체제와 스케줄링.md              완료
    │       │   │   ├── 3. 동기화 메커니즘(세마포어·뮤텍스·데드락).md 완료
    │       │   │   ├── 4. 제어 응답 특성 지표 전반.md                완료
    │       │   │   ├── 5. 로봇 모션 기초(좌표계·보간법).md           완료
    │       │   │   ├── 6. 센서 피드백과 신호처리.md                  완료
    │       │   │   ├── 7. 로봇 제어기 통신 프로토콜 심화.md          완료
    │       │   │   ├── 8. 안전 회로와 기능안전.md                    완료
    │       │   │   └── 9. 제어기 유형별 비교(산업용·협동·이동로봇).md 완료
    │       │   └── 2. 로봇 모니터링 프로그램/    (세세항목 — 아직 착수 안 함, 하위 페이지 없음)
    │       └── (2~5번 세부항목: 서버 연동 SW개발, 외부 툴 동기화 SW개발, 로봇 응용 SW개발, 로봇 훈련 SW개발 — 아직 폴더/페이지 미생성)
    ├── 로봇소프트웨어구조설계/
    │   └── 로봇소프트웨어개발기사_필기_2과목_문제은행_1.json   (세부 폴더/페이지 없음, JSON만 존재)
    ├── 모션소프트웨어/
    │   └── 로봇소프트웨어개발기사_필기_3과목_문제은행_1.json
    └── 지능소프트웨어/
        └── 로봇소프트웨어개발기사_필기_4과목_문제은행_1.json

지식창고/Box/     ← 용어 스텁 페이지 전용 폴더 (5절 참고, 현재 약 73개)

.obsidian/snippets/Cornell.css   ← 코넬노트 레이아웃 CSS (6절 참고)
```

**"1. 로봇 제어기 특성" 세세항목이 유일하게 1~9번 페이지가 모두 완성된 템플릿**이다. 나머지 모든 세세항목(1과목의 나머지 4개, 2~4과목 전체)은 이 패턴을 그대로 따라 만들어야 한다.

---

## 3. 문제은행 JSON 파일 형식

경로: 각 과목 폴더 최상위, 파일명 `로봇소프트웨어개발기사_필기_N과목_문제은행_1.json` (N=1~4)

```json
{
  "과목명": {
    "세부항목명(예: 1. 모니터링 툴 개발)": {
      "세세항목명(예: 1. 로봇 제어기 특성)": [
        "문제 지문 1 (번호 포함, 선택지·해설 없음)",
        "문제 지문 2"
      ]
    }
  }
}
```

- **선택지(①②③④)와 해설은 모두 제거된 상태** — 순수 문제 지문만 리스트로 들어있다.
- 문항 수: 1과목 124 / 2과목 119 / 3과목 72 / 4과목 105 (총 420문항)
- JSON의 "세부항목" 키는 실제 출제기준 원문 기준으로는 **"주요항목"에 해당하는 경우가 많다** (1과목만 주요항목이 1개라 세부항목과 사실상 동일 계층). 2~4과목은 원래 과목→주요항목→세부항목→세세항목의 4단계 구조이지만 이 JSON은 중간 단계를 생략하고 "주요항목→세세항목"으로 단순화했다. 실제 계층은 출제기준 원문과 대조해 확인할 것.
- 일부 문항은 원본 문제집에서 과목이 잘못 섞여 있던 것을 실제 내용에 맞게 재분류한 것들이 포함되어 있다.

---

## 4. 코넬노트 페이지 작성 규칙 (매우 중요 — 세션 중 확정된 규정)

### 4-1. Frontmatter
```yaml
---
cssclasses: cornell-note
tags: [로봇소프트웨어개발기사, <과목명>, <세부항목명 축약>]
과목: <과목명>
주요항목: <주요항목명>
세부항목: <세부항목명>
세세항목: <세세항목명>
---
```

### 4-2. 본문 구조
```markdown
# Summary
(1~2문단, 이 페이지 전체를 관통하는 핵심 요약)

---

<div class="cues-header">Cues</div>

# Notes

<aside>소주제 1</aside>
(설명, 불릿, 필요시 표)

<aside>소주제 2</aside>
...

---

<aside>핵심 정리</aside>
(불릿 요약)

---

<aside>관련 노트</aside>
- 개요(허브): [[상위/형제 페이지]]
- 관련: [[다른 세부 페이지]]
- 용어: [[Box 용어]]
```

### 4-3. 기출 문제를 절대 직접 인용하지 않는다
`> 기출: "..." → 정답: ...` 형식의 인용 블록은 사용하지 않기로 확정됨(세션 중 사용자가 명시적으로 지시하여 기존 페이지들도 전부 제거함). **문제 은행은 어떤 용어·개념을 다뤄야 하는지 파악하는 "소재 힌트"로만 사용**하고, 오답 함정이 있으면 이를 본문 서술 안에 자연스럽게 녹여서 설명한다 (예: "OO는 △△가 아니라 □□이다"라는 식의 correcting 서술).

### 4-4. Cue(aside)는 최대한 잘게 쪼갠다
하나의 aside에 여러 하위 개념을 불릿로 나열하지 않고, **개념 하나당 하나의 `<aside>제목</aside>` 블록**으로 분리하는 것을 선호함 (세션 중 "좌표계의 종류에서 월드 좌표계, 베이스 좌표계 등을 각각의 Cue로 설정하라"는 지시로 확정). 예: 5번 페이지는 좌표계 개념 설명 1개 + 좌표계 4종류 각각 + 구분선 + 보간법 개념 설명 1개 + 보간법 3종류 각각 = 총 9개의 개별 aside로 구성됨.

### 4-5. 수식은 LaTeX($...$, $$...$$)를 사용
Obsidian 코어 MathJax 기능을 사용. 아래첨자·위첨자는 HTML `<sub>`/`<sup>`가 아니라 `$t_r$`, `$e_{ss}$`처럼 LaTeX 문법으로 작성한다(세션 중 HTML 태그 방식에서 LaTeX 방식으로 전환 확정).

### 4-6. 표(table) 렌더링 버그 주의
**표 바로 위 줄이 볼드체(`**텍스트**`)나 일반 텍스트일 경우 반드시 빈 줄을 하나 넣을 것.** 빈 줄 없이 표가 바로 이어지면 Obsidian 읽기 모드에서 `|---|---|` 형태의 raw 텍스트로 보이는 렌더링 버그가 실제로 발생했다.

### 4-7. 표에 있는 내용을 본문에서 중복 서술하지 않는다
비교표를 만든 뒤, 그 표의 각 셀 내용을 별도 불릿으로 다시 풀어서 설명하지 않는다(세션 중 "표에 이미 있는 내용이며 보완해주는 내용도 없다"는 피드백으로 4번 페이지의 중복 불릿 2개를 삭제함). 표에 없는 **추가 정보(실무 팁, 결합 사용 사례 등)만** 표 아래에 덧붙인다.

### 4-8. 예시(example)는 실제 로봇 제어 맥락으로 구체화
추상적 개념 설명 뒤에는 "로봇 제어기에서의 예시"를 별도로 넣는 것을 선호함(3번 페이지 데드락 예시: 모션 제어 스레드/통신 처리 스레드가 뮤텍스 A/B를 반대 순서로 잠그는 상황 등). 개념 설명 아래 소제목 없이 자연스럽게 "**예시 — ...**" 형태로 삽입.

### 4-9. 요청 이후에도 "예상 기출을 벗어난" 보강은 허용
사용자가 실제 문제 수보다 더 풍부한 페이지 구성(예: 9개 페이지, 안전회로/제어기유형비교처럼 기출에 없는 페이지, 통신 프로토콜 20개 이상 나열 등)을 여러 차례 명시적으로 요청함 — "예상 기출 문제를 떠나서 고려하였을 때에도 충분한가?"라는 질문에 실무적으로 부족한 부분을 스스로 짚어 제안하는 방식이 잘 작동했다. 즉 **기출 커버리지 + 실무 완성도** 두 기준을 함께 고려해서 콘텐츠 볼륨을 판단할 것.

---

## 5. Box 용어 스텁 페이지 시스템

경로: `지식창고/Box/`

**규칙**:
- 본문 작성 중 별도 설명이 필요한 용어가 나오면, **Box에 이미 있는지 먼저 확인**하고, 없을 때만 새로 만든다.
- 새로 만드는 Box 페이지는 **frontmatter(태그)만 채우고 본문은 비워둔다.**
```markdown
---
cssclasses: cornell-note
tags:
  - 로봇소프트웨어개발기사
  - <상위분류1>
  - <상위분류2>
---
```
- 파일명에 `/`는 사용 불가(경로 구분자) → 언더스코어 등으로 치환 후 `[[파일명|표시텍스트]]` 별칭 문법으로 링크 (예: `[[TCP_IP|TCP/IP]]`).
- Obsidian이 위키링크 생성 시 자동으로 빈 파일을 만들어두는 경우가 있음(예: EDF) — 이미 존재하면 태그만 채워 넣는 식으로 처리.
- **본문 내용은 아직 전부 비어있는 상태** — 나중에 채워야 함. (사용자가 "계산 방법은 각 용어 페이지 쪽으로 넣는 게 나을 것 같다"고 언급한 적 있음 — 시정수/상승시간/정착시간/오버슈트/정상상태오차 등의 계산식을 Box 페이지 본문에 넣는 작업이 남아있음.)

**현재까지 생성된 Box 페이지 수: 약 73개** (RTOS/PID 제어/CAN/EtherCAT/Modbus/Profinet/PROFIBUS/CANopen/DeviceNet/SERCOS/POWERLINK/CC-Link/ControlNet/AS-Interface(ASi)/IO-Link/RS-232/RS-422/RS-485/I2C/SPI/USB/Wi-Fi/Bluetooth/Zigbee/LoRaWAN/TCP_IP/MQTT/ROS(Robot Operating System)/뮤텍스(Mutex)/세마포어(Semaphore)/임계영역(Critical Section)/데드락(Deadlock)/우선순위 역전(Priority Inversion)/인터럽트 서비스 루틴(ISR)/WCET(Worst Case Execution Time)/Earliest Deadline First(EDF)/VxWorks/QNX/RT-Linux/Xenomai/PREEMPT_RT/FreeRTOS/엔코더(Encoder)/이동평균 필터(Moving Average Filter)/칼만 필터(Kalman Filter)/저역통과 필터(Low-pass Filter)/시정수(Time Constant)/상승시간(Rise Time)/정착시간(Settling Time)/오버슈트(Overshoot)/정상상태오차(Steady-State Error)/피드백 제어(Feedback Control)/피드포워드 제어(Feedforward Control)/로봇 좌표계(Coordinate System)/보간법(Interpolation)/E-stop(비상정지)/SIL(Safety Integrity Level)/BLDC 모터/FOC(Field-Oriented Control)/PDO(Process Data Object)/SCADA/필드버스(Fieldbus)/협동로봇(Cobot)/컴플라이언스 제어(Compliance Control) 등). 정확한 최신 목록은 `지식창고/Box/` 디렉토리를 직접 조회해서 확인할 것.

- 그 외 기존에 있던 `kerberos.md`, `nud의 noarp와 perm의 차이.md`는 이 프로젝트와 무관한 기존 자료이며 내용이 채워진 정상 노트다 — 형식 참고용으로만 확인했었음.

---

## 6. Cornell 노트 CSS 스니펫

경로: `.obsidian/snippets/Cornell.css`

세션 중 아래와 같이 수정함:
- `--cue-width: 120px` → **200px**로 확장
- `.markdown-preview-sizer`의 `margin-inline: 110px` → **190px**로 동반 확장 (본문 시작 위치를 넓어진 Cue 폭에 맞춤)
- `aside`와 `div.cues-header` 모두 `width: fit-content; max-width: var(--cue-width);`로 통일 — Cue 텍스트가 짧으면 좁게, 길면 200px 한도까지 자동으로 늘어나는 shrink-to-fit 동작

CSS 원본은 레포의 `.obsidian/snippets/Cornell.css` 파일을 직접 확인할 것. 이 스니펫이 Obsidian 설정(모양 → CSS 스니펫)에서 활성화되어 있어야 코넬노트 레이아웃이 정상 렌더링된다.

---

## 7. 진행 상황 요약

| 항목 | 상태 |
|---|---|
| 1~4과목 문제은행 JSON (선택지/해설 제거) | 완료 |
| 1과목 "1. 로봇 제어기 특성" 1~9번 페이지 | 전체 완료 |
| Box 용어 스텁 페이지 (~73개) | 생성됨, 본문 내용은 전부 비어있음 — 추후 채워야 함 |
| Cornell.css 레이아웃 커스터마이징 | 완료 |
| 1과목의 나머지 세세항목(로봇 모니터링 프로그램 등 4개) | 폴더/페이지 자체가 없음, 처음부터 설계 필요 |
| 1과목의 나머지 세부항목(2~5번: 서버연동/외부툴동기화/응용SW/훈련SW) | 미착수 |
| 2·3·4과목 전체 (세부항목/세세항목 폴더 구조, 코넬노트 페이지) | 전혀 미착수, JSON만 존재 |

**작업 방식(사용자 선호)**: 페이지를 한 번에 여러 개 진행하지 않고, **한 페이지씩 작성 → 사용자 피드백 → 다음 페이지**로 진행하는 것을 기본으로 하되, 사용자가 "나머지 페이지도 계속 진행"처럼 명시적으로 이어서 진행을 지시하면 여러 페이지를 연속으로 작성해도 됨. 완료 후에는 항상 무엇을 했는지 요약 보고할 것.

---

## 8. Claude Code 작업 시 유의사항

1. Git 레포(`https://github.com/Tiemp-B/ObsidianBox.git`)를 clone한 **로컬 경로**에서 작업. 이 레포가 Obsidian 볼트 루트인지, 볼트의 하위 폴더인지 먼저 확인할 것.
2. 파일명에 한글, 특수문자(·, ( ), - 등)가 많이 쓰인다. `/`만 파일명에 사용 불가하므로 4절/5절의 치환 규칙을 지킬 것.
3. JSON 파일을 프로그램적으로 다룰 때는 UTF-8, `ensure_ascii=False`, `indent=2`로 저장할 것.
4. `.obsidian/` 폴더(설정, 스니펫)도 레포에 포함되어 있는지 확인. 포함되어 있지 않다면 Cornell.css를 별도로 전달받아야 함.
5. 새 세세항목 작업을 시작할 때 순서:
   - 해당 세세항목의 JSON 문제 리스트 확인
   - 주제별 클러스터링 → 페이지 분할안 제시 → 사용자 확인
   - 승인된 분할안대로 빈 스텁 페이지 먼저 생성(프론트매터만)
   - 이후 1페이지씩 4절의 작성 규칙을 지켜 내용 작성 + Box 용어 링크/생성 + 사용자 피드백 루프
6. Box 용어 페이지 본문을 채우는 작업(현재 전부 비어있음)이 언젠가 필요하며, 우선순위는 사용자에게 확인할 것.

---

## 9. 추가로 필요할 수 있는 정보 (확인 요청)

아래 항목들은 이 문서만으로는 알 수 없어서, Claude Code 세션 시작 시 사용자에게 직접 확인이 필요합니다:

1. **Git 레포와 실제 Obsidian 볼트의 관계**: 레포 루트가 볼트 루트와 동일한가, 아니면 볼트 안의 특정 하위 폴더(`지식창고/` 등)만 이 레포로 관리되는가?
2. **레포에 `.obsidian/` 설정 폴더가 포함되어 커밋되어 있는가?** (Cornell.css 스니펫이 이미 포함돼 있는지, 아니면 별도로 심어야 하는지)
3. **로컬 클론 경로**: Claude Code가 작업할 절대 경로
4. **Git 커밋/푸시 정책**: 페이지를 만들 때마다 커밋할지, 세션 끝에 한 번에 커밋할지, 커밋 메시지 컨벤션이 있는지
5. **다음 작업 우선순위**: (a) Box 용어 페이지 본문 채우기, (b) 1과목 나머지 세세항목/세부항목, (c) 2~4과목 착수 — 이 중 무엇부터 진행할지
